import 'package:flutter/material.dart';

class MonthScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Isi dengan tampilan halaman month.dart
    return Scaffold(
      appBar: AppBar(
        title: Text("Month Screen"),
      ),
      body: Center(
        child: Text("Halaman Month"),
      ),
    );
  }
}